# Root-level .env.example
# This file documents all environment variables for the entire project.
# See server/.env.example for server-specific variables.

# ── Server (Railway) ──────────────────────────────────────────────────────────
# Set these in Railway → Variables:

SESSION_SECRET=change_this_to_a_strong_random_secret_123
# PORT is set automatically by Railway — do NOT set manually
